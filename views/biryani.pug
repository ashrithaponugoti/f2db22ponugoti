extends layout

block content
  h1= title
  - let biryani=results
  div(class="container-fluid")
  div(class="row")
   each b,index in biryani
    div(class="col-sm-2" onclick =`buy(${index})`) #{b.bir_quantity}, #{b.bir_cost}, #{b.bir_ing}
   script.
    function buy(i)
    {
      alert(`You purchased Item ${i+1}`)
    } 